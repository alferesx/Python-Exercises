valor_hora = float(input('Digite seu valor a hora: '))
horas_trabalhadas = float(input('Digite o valor de horas trabalhadas no mes: '))
salario_total = valor_hora * horas_trabalhadas
print('O salario do funcionario é: ',salario_total)
